<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
        The printable data sheet. Kept in the toolbar rather than buried in the
        page because an agent reaches for it on the doorstep, one-handed, and
        often before they have finished reading the screen.
    -->
    <item
        android:id="@+id/action_download_sheet"
        android:icon="@drawable/ic_download"
        android:title="@string/customer_download_sheet"
        app:showAsAction="ifRoom" />
</menu>
