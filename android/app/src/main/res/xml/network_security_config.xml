<?xml version="1.0" encoding="utf-8"?>
<!--
    Production traffic is HTTPS only.

    Cleartext is permitted for localhost and the standard emulator host alias so
    a developer can point the app at a local PHP server, without weakening the
    policy for real deployments.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- The host machine as seen from the Android emulator. -->
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>
</network-security-config>
